# Shell scripts MUST keep LF, or they break on macOS/Linux (bad interpreter).
*.sh text eol=lf
# PowerShell scripts use CRLF on checkout (Windows-native).
*.ps1 text eol=crlf
# Everything else: normalize to LF in the repo, native on checkout.
* text=auto
